<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logged Out - MeshNet</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .logout-container {
            width: 100%;
            max-width: 500px;
            padding: 3rem 2rem;
            background: white;
            border-radius: 10px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            text-align: center;
        }
        .logout-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
            animation: fadeIn 0.5s ease-in;
        }
        .logout-title {
            font-size: 2rem;
            color: #667eea;
            margin-bottom: 1rem;
            font-weight: 600;
        }
        .logout-message {
            color: #666;
            font-size: 1.1rem;
            margin-bottom: 2rem;
            line-height: 1.6;
        }
        .logout-details {
            background: #f5f5f5;
            padding: 1.5rem;
            border-radius: 8px;
            margin-bottom: 2rem;
            text-align: left;
        }
        .logout-details p {
            margin: 0.5rem 0;
            color: #555;
        }
        .logout-details strong {
            color: #333;
        }
        .logout-btn {
            display: inline-block;
            padding: 0.75rem 2rem;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: 600;
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .logout-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
            color: white;
        }
        .logout-info {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 2px solid #e0e0e0;
            font-size: 0.9rem;
            color: #888;
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.8);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }
    </style>
</head>
<body>
    <div class="logout-container">
        <div class="logout-icon">👋</div>
        
        <h1 class="logout-title">You've Been Logged Out</h1>
        
        <p class="logout-message">
            <%= message %>
        </p>

        <div class="logout-details">
            <p><strong>✓ Session Closed</strong></p>
            <p>Your session has been securely terminated and removed from the server.</p>
            <p style="margin-top: 1rem;"><strong>✓ Data Protected</strong></p>
            <p>All authentication tokens have been invalidated for your security.</p>
        </div>

        <a href="/login" class="logout-btn">Login Again</a>

        <div class="logout-info">
            <p>If you did not log out intentionally, please log in again and change your password.</p>
            <p style="margin-top: 0.5rem;">For security concerns, please contact the system administrator.</p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
