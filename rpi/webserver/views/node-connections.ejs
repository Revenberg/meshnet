<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - MeshNet</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 1400px;
      margin: 0 auto;
    }

    .header {
      text-align: center;
      color: white;
      margin-bottom: 30px;
    }

    .header h1 {
      font-size: 2.5em;
      margin-bottom: 10px;
      text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
    }

    .header p {
      font-size: 1.1em;
      opacity: 0.9;
    }

    .nodes-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(400px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }

    .node-card {
      background: white;
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .node-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
    }

    .node-header {
      display: flex;
      align-items: center;
      margin-bottom: 15px;
      padding-bottom: 15px;
      border-bottom: 2px solid #f0f0f0;
    }

    .node-icon {
      font-size: 2em;
      margin-right: 12px;
    }

    .node-info h2 {
      font-size: 1.3em;
      color: #333;
      margin-bottom: 5px;
    }

    .node-id {
      font-size: 0.85em;
      color: #999;
      font-family: monospace;
      word-break: break-all;
    }

    .node-meta {
      display: flex;
      gap: 15px;
      margin-bottom: 15px;
      font-size: 0.9em;
    }

    .meta-item {
      display: flex;
      align-items: center;
      gap: 5px;
    }

    .meta-label {
      color: #666;
      font-weight: 600;
    }

    .meta-value {
      color: #333;
    }

    .status-badge {
      display: inline-block;
      padding: 4px 8px;
      border-radius: 4px;
      font-size: 0.8em;
      font-weight: 600;
      text-transform: uppercase;
    }

    .status-online {
      background: #d4edda;
      color: #155724;
    }

    .status-offline {
      background: #f8d7da;
      color: #721c24;
    }

    .connections-section {
      margin-top: 15px;
    }

    .connections-title {
      font-size: 0.9em;
      font-weight: 600;
      color: #667eea;
      margin-bottom: 10px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .connections-list {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .connection-item {
      display: flex;
      align-items: center;
      padding: 10px;
      background: #f8f9fa;
      border-left: 3px solid #667eea;
      border-radius: 4px;
      font-size: 0.9em;
    }

    .connection-icon {
      font-size: 1.2em;
      margin-right: 8px;
    }

    .connection-info {
      flex: 1;
    }

    .connection-user {
      font-weight: 600;
      color: #333;
    }

    .connection-team {
      font-size: 0.85em;
      color: #666;
    }

    .connection-time {
      font-size: 0.8em;
      color: #999;
      margin-left: auto;
      text-align: right;
    }

    .team-badge {
      display: inline-block;
      padding: 3px 6px;
      background: #e8eaf6;
      color: #3f51b5;
      border-radius: 3px;
      font-size: 0.75em;
      font-weight: 600;
      margin-top: 2px;
    }

    .no-connections {
      padding: 15px;
      background: #f8f9fa;
      border-radius: 4px;
      color: #999;
      text-align: center;
      font-style: italic;
    }

    .error-alert {
      background: #f8d7da;
      border: 1px solid #f5c6cb;
      color: #721c24;
      padding: 15px;
      border-radius: 8px;
      margin-bottom: 20px;
    }

    .stats-row {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 15px;
      margin-bottom: 30px;
    }

    .stat-card {
      background: white;
      padding: 15px;
      border-radius: 8px;
      text-align: center;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .stat-value {
      font-size: 2em;
      font-weight: 700;
      color: #667eea;
    }

    .stat-label {
      font-size: 0.9em;
      color: #666;
      margin-top: 5px;
    }

    .refresh-btn {
      position: fixed;
      bottom: 30px;
      right: 30px;
      width: 60px;
      height: 60px;
      background: #667eea;
      color: white;
      border: none;
      border-radius: 50%;
      font-size: 1.5em;
      cursor: pointer;
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
      transition: all 0.3s;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .refresh-btn:hover {
      transform: scale(1.1);
      box-shadow: 0 6px 16px rgba(102, 126, 234, 0.6);
    }

    .back-link {
      color: white;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 20px;
      padding: 10px 15px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 6px;
      transition: background 0.3s;
    }

    .back-link:hover {
      background: rgba(255, 255, 255, 0.2);
    }

    @media (max-width: 768px) {
      .nodes-grid {
        grid-template-columns: 1fr;
      }

      .header h1 {
        font-size: 1.8em;
      }

      .node-meta {
        flex-direction: column;
        gap: 8px;
      }

      .connection-time {
        margin-left: 0;
        text-align: left;
        margin-top: 5px;
      }

      .refresh-btn {
        width: 50px;
        height: 50px;
        font-size: 1.2em;
        bottom: 20px;
        right: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <a href="/" class="back-link">← Dashboard</a>

    <div class="header">
      <h1>🌐 Node Team Connections</h1>
      <p>Track which teams and users are connected to each mesh node</p>
    </div>

    <% if (error) { %>
      <div class="error-alert">
        <strong>Error:</strong> <%= error %>
      </div>
    <% } %>

    <div class="stats-row">
      <div class="stat-card">
        <div class="stat-value"><%= nodes.length %></div>
        <div class="stat-label">Total Nodes</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">
          <%= nodes.reduce((sum, n) => sum + (n.connections ? n.connections.length : 0), 0) %>
        </div>
        <div class="stat-label">Total Connections</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">
          <%= nodes.filter(n => n.connections && n.connections.length > 0).length %>
        </div>
        <div class="stat-label">Active Nodes</div>
      </div>
    </div>

    <div class="nodes-grid">
      <% if (nodes && nodes.length > 0) { %>
        <% nodes.forEach(node => { %>
          <div class="node-card">
            <div class="node-header">
              <div class="node-icon">📍</div>
              <div class="node-info">
                <h2><%= node.functionalName || 'Node ' + node.nodeId.substring(0, 8) %></h2>
                <div class="node-id"><%= node.nodeId %></div>
              </div>
            </div>

            <div class="node-meta">
              <div class="meta-item">
                <span class="meta-label">Status:</span>
                <span class="meta-value">
                  <% if (new Date(node.lastSeen) > new Date(Date.now() - 60000)) { %>
                    <span class="status-badge status-online">Online</span>
                  <% } else { %>
                    <span class="status-badge status-offline">Offline</span>
                  <% } %>
                </span>
              </div>
              <div class="meta-item">
                <span class="meta-label">Battery:</span>
                <span class="meta-value"><%= node.battery ? node.battery + '%' : 'N/A' %></span>
              </div>
            </div>

            <div class="connections-section">
              <div class="connections-title">
                Connected Teams & Users
              </div>

              <% if (node.connections && node.connections.length > 0) { %>
                <div class="connections-list">
                  <% node.connections.forEach(conn => { %>
                    <div class="connection-item">
                      <div class="connection-icon">👤</div>
                      <div class="connection-info">
                        <div class="connection-user"><%= conn.username %></div>
                        <% if (conn.teamName) { %>
                          <span class="team-badge"><%= conn.teamName %></span>
                        <% } %>
                        <div class="connection-team">
                          First: <%= new Date(conn.firstSeen).toLocaleString() %><br>
                          Last: <%= new Date(conn.lastSeen).toLocaleString() %>
                        </div>
                      </div>
                    </div>
                  <% }); %>
                </div>
              <% } else { %>
                <div class="no-connections">
                  📭 No team connections recorded
                </div>
              <% } %>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <div style="grid-column: 1/-1; text-align: center; padding: 40px; color: white;">
          <p style="font-size: 1.2em;">No nodes available</p>
        </div>
      <% } %>
    </div>
  </div>

  <button class="refresh-btn" onclick="location.reload();" title="Refresh connections">🔄</button>

  <script>
    // Auto-refresh every 30 seconds
    setInterval(() => {
      location.reload();
    }, 30000);
  </script>
</body>
</html>
